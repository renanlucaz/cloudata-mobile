import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        borderColor: "#ccc",
        borderWidth: 1,
        minWidth: "100%",
        paddingVertical: 18,
        paddingHorizontal: 16,
        borderRadius: 8,
        fontSize: 14
    }
})