import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        borderColor: "#e0e0e0",
        borderWidth: 1,
        minWidth: "100%",
        paddingVertical: 10,
        paddingHorizontal: 16,
        borderRadius: 8,
        fontSize: 14
    }
})